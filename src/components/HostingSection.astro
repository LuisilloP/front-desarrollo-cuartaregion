<!-- DEVELOPER_NOTE: This component is about "Use Cases". For clarity, it would be best to rename this file to "UseCasesSection.astro" and update the import in the parent page. -->
---
import { Icon } from "astro-icon/components";
import type { UseCasesSectionContent } from "../lib/api/types";

interface Props {
  content: UseCasesSectionContent;
}

const { content } = Astro.props;
---

<section id="use-cases" class="py-20 lg:py-32">
  <div class="container mx-auto px-6 sm:px-8">
    <!-- Section Header -->
    <div class="mx-auto mb-16 max-w-3xl text-center">
      <div class="mb-4 inline-flex items-center gap-2 rounded-full px-4 py-2 text-sm font-medium
      bg-amber-500/10 text-amber-600
      dark:bg-amber-400/10 dark:text-amber-300">
        <Icon name="lucide:layout-grid" class="h-4 w-4" />
        {content.eyebrow}
      </div>
      <h2 class="text-4xl font-bold tracking-tight text-slate-900 dark:text-white sm:text-5xl">
        {content.title}
      </h2>
      <p class="mt-4 text-lg text-slate-600 dark:text-white/60">
        {content.description}
      </p>
    </div>

    <!-- Reasons Grid -->
    <div id="use-cases-grid" class="grid grid-cols-1 gap-8 lg:grid-cols-3">
      {content.cases.map((useCase, index) => {
        const accentClass = index === 0
          ? "accent-blue"
          : index === 1
            ? "accent-amber"
            : "accent-purple";
        return (
        <div class="use-case-card-wrapper h-full">
          <div
            class={`card-shine ${accentClass} group relative flex h-full flex-col rounded-3xl border p-8 backdrop-blur-xl transition-all duration-300 overflow-hidden
          border-slate-900/40 bg-slate-950/85 hover:border-slate-400/40 hover:!bg-slate-950/90
          dark:border-white/10 dark:bg-slate-950/80 dark:hover:border-white/20 dark:hover:!bg-slate-950/85`}
          >
            
            {/* Background image for this card */}
            <div 
              class="absolute inset-0 bg-[url('/images/sections/why-us-bg.png')] bg-cover bg-center opacity-15 scale-105 -z-10"
              aria-hidden="true"
            ></div>

            {/* Overlay for readability - lighter in light mode, darker in dark mode */}
            <div class={`use-case-tint absolute inset-0 ${accentClass} -z-10`}></div>

            {/* Content */}
            <div class="relative z-10">
              <!-- Large Number -->
              <div class="mb-6 text-6xl font-bold text-white/10 select-none font-mono">
                0{index + 1}
              </div>

              <h3 class="text-xl font-bold leading-snug tracking-tight text-white mb-3">
                {useCase.title}
              </h3>
              
              <p class="text-white/70 line-clamp-3 mb-6">
                {useCase.description}
              </p>

              {useCase.bullets && useCase.bullets.length > 0 && (
                <ul class="mt-auto space-y-3 border-t border-white/10 pt-6">
                  {useCase.bullets.map((bullet) => (
                    <li class="flex items-start gap-3 text-sm text-white/70">
                      <Icon name="lucide:check" class="use-case-check mt-0.5 h-4 w-4 shrink-0" />
                      <span>{bullet}</span>
                    </li>
                  ))}
                </ul>
              )}
            </div>
          </div>
        </div>
        );
      })}
    </div>

    <!-- Closing Text -->
    {content.closingText && (
      <div class="mt-16 text-center">
        <p class="mx-auto max-w-2xl text-lg font-medium text-slate-500 dark:text-white/40">
          {content.closingText}
        </p>
      </div>
    )}
  </div>
</section>

<style>
  .use-case-card-wrapper {
    opacity: 0;
    transform: translateY(30px);
    transition: opacity 0.6s cubic-bezier(0.25, 0.46, 0.45, 0.94), transform 0.6s cubic-bezier(0.25, 0.46, 0.45, 0.94);
    transition-delay: var(--delay, 0s);
  }
  .use-case-card-wrapper.is-visible {
    opacity: 1;
    transform: translateY(0);
  }

  /* Card shine effect (re-scoped here to avoid conflicts if scripts get bundled) */
  .card-shine {
    overflow: hidden;
  }
  .card-shine::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: radial-gradient(
      400px circle at var(--mouse-x, -9999px) var(--mouse-y, -9999px),
      var(--shine-color, rgba(100, 116, 139, 0.1)),
      transparent 40%
    );
    border-radius: inherit;
    z-index: 0;
    transition: opacity 0.3s;
  }

  /* Accent variants: 01 blue, 02 orange, 03 purple */
  .accent-blue {
    --shine-color: rgba(56, 189, 248, 0.12);
  }
  .accent-amber {
    --shine-color: rgba(251, 146, 60, 0.14);
  }
  .accent-purple {
    --shine-color: rgba(168, 85, 247, 0.14);
  }
  .use-case-tint {
    background: linear-gradient(135deg, rgba(2, 6, 23, 0.11), rgba(2, 6, 23, 0.3), rgba(2, 6, 23, 0.09));
  }
  .dark .use-case-tint {
    background: linear-gradient(135deg, rgba(2, 6, 23, 0.11), rgba(2, 6, 23, 0.3), rgba(2, 6, 23, 0.09));
  }
  .accent-blue.use-case-tint {
    background: linear-gradient(135deg, rgba(14, 165, 233, 0.11), rgba(2, 6, 23, 0.3), rgba(56, 189, 248, 0.09));
  }
  .dark .accent-blue.use-case-tint {
    background: linear-gradient(135deg, rgba(14, 165, 233, 0.11), rgba(2, 6, 23, 0.3), rgba(56, 189, 248, 0.09));
  }
  .accent-amber.use-case-tint {
    background: linear-gradient(135deg, rgba(251, 146, 60, 0.11), rgba(2, 6, 23, 0.3), rgba(245, 158, 11, 0.09));
  }
  .dark .accent-amber.use-case-tint {
    background: linear-gradient(135deg, rgba(251, 146, 60, 0.11), rgba(2, 6, 23, 0.3), rgba(245, 158, 11, 0.09));
  }
  .accent-purple.use-case-tint {
    background: linear-gradient(135deg, rgba(168, 85, 247, 0.11), rgba(2, 6, 23, 0.3), rgba(139, 92, 246, 0.09));
  }
  .dark .accent-purple.use-case-tint {
    background: linear-gradient(135deg, rgba(168, 85, 247, 0.11), rgba(2, 6, 23, 0.3), rgba(139, 92, 246, 0.09));
  }
  .accent-blue .use-case-check {
    color: rgba(56, 189, 248, 0.9); /* sky-400 */
  }
  .accent-amber .use-case-check {
    color: rgba(251, 146, 60, 0.9); /* orange-400 */
  }
  .accent-purple .use-case-check {
    color: rgba(168, 85, 247, 0.9); /* purple-500 */
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    // Staggered Animation
    const grid = document.getElementById('use-cases-grid');
    if (grid) {
      const cards = grid.querySelectorAll('.use-case-card-wrapper');
      const observer = new IntersectionObserver((entries, obs) => {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            cards.forEach((card, index) => {
              const cardElement = card as HTMLElement;
              cardElement.style.setProperty('--delay', `${index * 100}ms`);
              cardElement.classList.add('is-visible');
            });
            obs.unobserve(grid);
          }
        });
      }, { threshold: 0.1, rootMargin: '0px 0px -50px 0px' });
      observer.observe(grid);
    }

    // Card Shine Effect
    function setupCardShine() {
      document.querySelectorAll('#use-cases-grid .card-shine').forEach(card => {
        const htmlCard = card as HTMLElement;
        if (htmlCard.dataset.shineListener === 'true') return;
        
        htmlCard.addEventListener('mousemove', (e) => {
          const mouseEvent = e as MouseEvent;
          const rect = htmlCard.getBoundingClientRect();
          const x = mouseEvent.clientX - rect.left;
          const y = mouseEvent.clientY - rect.top;
          htmlCard.style.setProperty('--mouse-x', `${x}px`);
          htmlCard.style.setProperty('--mouse-y', `${y}px`);
        });
        htmlCard.dataset.shineListener = 'true';
      });
    }
    setupCardShine();
    document.addEventListener('astro:after-swap', setupCardShine);
  });
</script>
