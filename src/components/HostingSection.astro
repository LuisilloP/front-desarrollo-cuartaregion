---
import type { SiteSettings } from "../data/mock";
import { buildWhatsAppLink } from "../lib/utils";

interface Props {
  siteSettings: SiteSettings;
}

const { siteSettings } = Astro.props;
const whatsappUrl = buildWhatsAppLink(siteSettings.whatsappNumber, "Quiero hosting administrado y mantencion integral.");
---

<section class="py-16" data-animate="reveal">
  <div class="section-shell card bg-card/90 border-border grid md:grid-cols-2 gap-8 items-center">
    <div class="space-y-4">
      <p class="pill bg-primary/10 border-primary/40 text-highlight">Operacion continua</p>
      <h3 class="text-2xl sm:text-3xl font-bold text-content">Hosting, mantencion y soporte para que todo corra fino</h3>
      <p class="text-muted">
        Incluimos monitoreo, certificados SSL renovados, backups y despliegues controlados. Tambien hacemos limpieza y ajustes de
        performance en tus equipos para que la experiencia digital y fisica sea consistente.
      </p>
      <ul class="grid sm:grid-cols-2 gap-3 text-sm text-muted">
        <li class="flex items-start gap-2">
          <span class="mt-1 h-2 w-2 rounded-full bg-accent"></span>Backups automaticos y restauraciones rapidas
        </li>
        <li class="flex items-start gap-2">
          <span class="mt-1 h-2 w-2 rounded-full bg-accent"></span>CDN y optimizacion de rendimiento
        </li>
        <li class="flex items-start gap-2">
          <span class="mt-1 h-2 w-2 rounded-full bg-accent"></span>Soporte local y priorizacion de incidencias
        </li>
        <li class="flex items-start gap-2">
          <span class="mt-1 h-2 w-2 rounded-full bg-accent"></span>Panel seguro con roles, bitacoras y auditoria
        </li>
      </ul>
    </div>
    <div class="space-y-4">
      <div class="p-5 rounded-2xl bg-gradient-to-br from-card to-surface-strong/60 border border-border">
        <p class="text-sm text-accent mb-2 font-semibold">Que incluye la mantencion fisica?</p>
        <ol class="space-y-3 text-muted text-sm">
          <li>1. Limpieza antiestatica, cable management y cambio de pasta termica.</li>
          <li>2. Stress test y ajustes de ventilacion para que tus sitios y estaciones no se caigan.</li>
          <li>3. Reporte mensual unificado (web + hardware) para tomar decisiones rapidas.</li>
        </ol>
      </div>
      <a
        href={whatsappUrl}
        target="_blank"
        rel="noreferrer"
        class="inline-flex items-center justify-center gap-2 px-5 py-3 rounded-full bg-gradient-to-r from-primary to-accent text-secondary font-semibold shadow-soft hover:shadow-lg transition w-full sm:w-auto"
      >
        Quiero operacion con mantencion
      </a>
    </div>
  </div>
</section>
