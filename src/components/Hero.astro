---
import type { SiteSettings } from "../data/mock";
import { buildWhatsAppLink } from "../lib/utils";
import { Icon } from "astro-icon/components";

interface Props {
  siteSettings: SiteSettings;
}

const { siteSettings } = Astro.props;
const region = siteSettings.region || "Region Metropolitana";
const whatsappUrl = buildWhatsAppLink(siteSettings.whatsappNumber, "Hola! Quiero una web y automatizacion para mi negocio.");
---

<section id="inicio" class="relative overflow-hidden pb-24 pt-12 sm:pt-20" data-animate="reveal">
  <div class="absolute inset-0 pointer-events-none">
    <div class="absolute -top-24 -left-10 w-80 h-80 bg-primary/20 rounded-full blur-3xl"></div>
    <div class="absolute top-10 right-0 w-96 h-96 bg-accent/20 rounded-full blur-3xl"></div>
  </div>
  <div class="section-shell relative grid lg:grid-cols-2 gap-12 items-center">
    <div class="space-y-6">
      <div class="pill w-fit bg-primary/10 border-primary/40 text-highlight">
        <span class="h-2 w-2 rounded-full bg-accent"></span>
        Agencia local - {region}
      </div>
      <h1 class="text-4xl sm:text-5xl font-bold text-content leading-tight">
        Sitios y automatizaciones de WhatsApp para negocios que quieren crecer
      </h1>
      <p class="text-lg text-muted max-w-2xl">
        Disenamos y lanzamos rapido: landings, reservas, bots y soporte TI para que tu equipo venda mas y dedique menos tiempo a
        tareas repetitivas.
      </p>
      <div class="flex flex-wrap gap-3">
        <a
          class="inline-flex items-center gap-3 px-6 py-3 rounded-full bg-gradient-to-r from-primary to-accent text-secondary font-semibold shadow-soft hover:shadow-lg transition"
          href={whatsappUrl}
          target="_blank"
          rel="noreferrer"
        >
          {siteSettings.primaryCTA || "Hablemos por WhatsApp"}
          <span aria-hidden="true">-></span>
        </a>
        <a
          class="inline-flex items-center gap-3 px-6 py-3 rounded-full bg-secondary text-contrast border border-border hover:border-highlight transition"
          href="#servicios"
        >
          {siteSettings.secondaryCTA || "Ver planes"}
        </a>
      </div>
      <div class="grid sm:grid-cols-3 gap-3 text-sm text-muted">
        <div class="card flex flex-col gap-2">
          <div class="flex items-center gap-2 text-content">
            <Icon name="lucide:rocket" class="h-5 w-5 flex-shrink-0 text-accent" aria-hidden="true" />
            <p class="font-semibold">Entrega rapida</p>
          </div>
          <p class="text-muted leading-relaxed">Landings y funnels listos en menos de 7 dias habiles.</p>
        </div>
        <div class="card flex flex-col gap-2">
          <div class="flex items-center gap-2 text-content">
            <Icon name="lucide:headset" class="h-5 w-5 flex-shrink-0 text-accent" aria-hidden="true" />
            <p class="font-semibold">Soporte local</p>
          </div>
          <p class="text-muted leading-relaxed">Equipo basado en la region, horario Chile y soporte directo.</p>
        </div>
        <div class="card flex flex-col gap-2">
          <div class="flex items-center gap-2 text-content">
            <Icon name="lucide:shield-check" class="h-5 w-5 flex-shrink-0 text-accent" aria-hidden="true" />
            <p class="font-semibold">Monitoreo y mantencion</p>
          </div>
          <p class="text-muted leading-relaxed">Backups, parches y limpieza programada para tu stack y equipos.</p>
        </div>
      </div>
    </div>
    <div class="relative">
      <div class="card bg-gradient-to-br from-card/90 to-card/70 border-border">
        <p class="text-sm text-accent mb-2 font-semibold">Checklist de lanzamiento</p>
        <ul class="space-y-3 text-content">
          <li class="flex items-start gap-3">
            <span class="h-6 w-6 rounded-full bg-accent/15 text-accent grid place-content-center">
              <span class="block h-2 w-2 rounded-full bg-accent"></span>
            </span>
            Diseno enfocado en conversion y SEO local.
          </li>
          <li class="flex items-start gap-3">
            <span class="h-6 w-6 rounded-full bg-accent/15 text-accent grid place-content-center">
              <span class="block h-2 w-2 rounded-full bg-accent"></span>
            </span>
            Integracion a WhatsApp, flujos y CRM sin codigo.
          </li>
          <li class="flex items-start gap-3">
            <span class="h-6 w-6 rounded-full bg-accent/15 text-accent grid place-content-center">
              <span class="block h-2 w-2 rounded-full bg-accent"></span>
            </span>
            Panel de edicion para contenido y precios.
          </li>
          <li class="flex items-start gap-3">
            <span class="h-6 w-6 rounded-full bg-accent/15 text-accent grid place-content-center">
              <span class="block h-2 w-2 rounded-full bg-accent"></span>
            </span>
            Monitoreo y respaldos listos para operaciones.
          </li>
        </ul>
      </div>
    </div>
  </div>
</section>
